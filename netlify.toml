# Production (coming-soon branch) — static landing page
[build]
  command = "echo 'Static site, no build needed'"
  publish = "static/coming-soon"

# Main branch — full Docusaurus site (deploy preview only until launch)
[context.main]
  command = "yarn build"
  publish = "build"
